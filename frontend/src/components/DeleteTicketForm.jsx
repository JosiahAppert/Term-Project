const DeleteTicketForm = ({ rowObject, backendURL, refreshTicket }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteTicketForm;