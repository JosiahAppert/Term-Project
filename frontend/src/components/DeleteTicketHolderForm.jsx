const DeleteTicketHolderForm = ({ rowObject, backendURL, refreshTicketHolder }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteTicketHolderForm;