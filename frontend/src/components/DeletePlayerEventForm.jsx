const DeletePlayerEventForm = ({ rowObject, backendURL, refreshPlayerEvent }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeletePlayerEventForm;